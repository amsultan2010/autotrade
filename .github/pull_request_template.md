## What changed

<!-- Brief summary -->

## Owner area

- [ ] `web/` — website (Abdullah)
- [ ] `engine/` or `worker/` — algorithm (Preston)
- [ ] `shared/` — cross-boundary types (both reviewed)

## Checklist

- [ ] Branch name matches area: `web/...`, `engine/...`, or `shared/...`
- [ ] Only touched files in my owned area (or coordinated `shared/` change)
- [ ] Ran local check: `pnpm typecheck:web` or `pnpm typecheck:engine`
- [ ] If `shared/` changed: both owners reviewed
- [ ] If `engine/src/public/` changed: web owner aware (API contract)

## Test plan

<!-- How you verified this works -->
