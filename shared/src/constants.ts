/** Max paper trades for users without an active Pro subscription. */
export const FREE_PAPER_TRADE_LIMIT = 10;

/** Default simulated paper account starting balance (USD). */
export const PAPER_STARTING_BALANCE = 100_000;
