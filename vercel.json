{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm exec prisma generate --schema packages/engine/prisma/schema.prisma && pnpm --filter @autotrade/web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
